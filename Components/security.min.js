function linkChecker(m){
    var currentLink = window.location;
    var currentHref = currentLink['href'];
    const afterQuestionMark = currentHref.split('?')[1];
    if(afterQuestionMark==''||afterQuestionMark==undefined){
        if(fetchDataBase()==true){
            if(DataBin==undefined){
                if(creatAccount()){
                    if(enivroment==true){
                        mainPageRoute(`IcA22fM1108H${timeEncoder(((new Date).getTime()),0)}`);
                    }
                }
            }
        }
    }
}
function creatAccount(){
    return true;
}
let enivroment=true;
function mainPageRoute(link){
    var currentLink = window.location;
    var currentHref = currentLink['href'];
    const url = new URL(currentHref);
    const params = url.searchParams;
    params.set('test',link);
    link = './main.html?page=HomePage&'+url.search.replace('?','')+'&env=false';
    window.location = link;
}
/*
{
    userName: 'Gest',
    userId: 'Guest@2401cA22f22K%5r08HW08h03f',
    frequency: 'f',
    other: ['22','88']
}
*/